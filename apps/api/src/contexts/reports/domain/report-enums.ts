export enum ReportType {
  Incident = 'incident',
  Stock = 'stock',
  Status = 'status',
  Other = 'other',
}

export enum ReportPriority {
  Low = 'low',
  Medium = 'medium',
  High = 'high',
  Urgent = 'urgent',
}

export enum ReportStatus {
  Open = 'open',
  Reviewed = 'reviewed',
}
